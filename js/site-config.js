/**
 * Party launch site — edit this file to control city copy.
 *
 * - `cities`: strings shown wherever [your city] appears. One entry = static text (no rotation).
 * - `cityRotateIntervalMs`: time between city changes when you have 2+ cities.
 */
window.PARTY_SITE_CONFIG = {
  cities: ['San Francisco', 'Los Angeles'],
  cityRotateIntervalMs: 4800,
};
